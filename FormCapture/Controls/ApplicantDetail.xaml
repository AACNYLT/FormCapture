<UserControl
    x:Class="FormCapture.Controls.ApplicantDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FormCapture.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <RelativePanel Background="{ThemeResource SystemControlChromeHighAcrylicWindowMediumBrush}" Padding="20">
        <Button x:Name="Back" Click="Back_Click" Style="{ThemeResource NavigationBackButtonNormalStyle}"></Button>
        <TextBlock x:Name="ApplicantHeader" Text="{x:Bind applicant.ToString()}" TextWrapping="NoWrap" RelativePanel.RightOf="Back" Style="{ThemeResource SubheaderTextBlockStyle}"></TextBlock>
    </RelativePanel>
</UserControl>
